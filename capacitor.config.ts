import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'pcii-grupo5',
  webDir: 'dist/pcii-grupo5/browser'
};

export default config;
